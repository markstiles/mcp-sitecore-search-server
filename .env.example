# Environment Configuration for Sitecore Search MCP Server

# Default Domain Configuration
# Set your primary Sitecore domain ID here (just the domain ID, not the full client key)
# The domain ID is the second part of your client key (after the dash)
# Example: If your client key is 123456789-987654321, then domain ID is 987654321
SITECORE_DOMAIN_ID=your-domain-id

# Client Key (Full key for Events API)
# Format: companyId-domainId (two numbers separated by a dash)
# First part: Company ID | Second part: Domain ID
# Get this from Developer Resources > API Access in Sitecore Search
# Example: SITECORE_CLIENT_KEY=123456789-987654321
#          where 123456789 is company ID and 987654321 is domain ID
# SITECORE_CLIENT_KEY=123456789-987654321

# Optional: Override default API base URLs
# For subdomain setup, use your subdomain base URL (found in Developer Resources > API Access)
# Provide ONLY the base URL without paths - the client will add paths like /discover/v2/{domainId}
# SITECORE_SEARCH_BASE_URL=https://discover.sitecorecloud.io
# SITECORE_INGESTION_BASE_URL=https://discover.sitecorecloud.io
# SITECORE_EVENTS_BASE_URL=https://discover.sitecorecloud.io

# Authentication Configuration
# API Key (52-character secret code from Sitecore Search)
# If you have a subdomain, authentication is handled automatically and this is optional
# Format: 01-xxxxxxxx-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Get this from Developer Resources > API Access in Sitecore Search
# SITECORE_API_KEY=01-xxxxxxxx-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional: Authentication Scopes (comma-separated)
# Available scopes: discover, event, ingestion
# Default: discover,event,ingestion
# SITECORE_AUTH_SCOPES=discover,event,ingestion

# Optional: Access Token Expiry (in milliseconds)
# Default: 86400000 (1 day)
# SITECORE_ACCESS_TOKEN_EXPIRY=86400000

# Optional: Refresh Token Expiry (in milliseconds)
# Default: 604800000 (7 days)
# SITECORE_REFRESH_TOKEN_EXPIRY=604800000

# Optional: Set default domain (if different from SITECORE_DOMAIN_ID)
# SITECORE_DEFAULT_DOMAIN=your-default-domain

# Advanced: Multiple Domains Configuration
# Use JSON format to configure multiple domains with individual auth settings
# SITECORE_DOMAINS_JSON={"domain1":{"searchBaseUrl":"https://...","apiKey":"01-...","authScopes":["discover","event"]},"domain2":{...}}

# Debug Mode
# Uncomment to enable debug logging
# DEBUG=true
